export const SET_PHOTOS = (state, photos) => {
    state.photos = photos
}

export const SET_PHOTOBYID = (state, photo) => {
    state.photo = photo
}

export const SET_ALBUMS = (state, albums) => {
    state.albums = albums
}

export const SET_ALBUMBYID = (state, album) => {
    state.album = album
}
